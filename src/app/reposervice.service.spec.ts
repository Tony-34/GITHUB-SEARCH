import { Reposervice.Service } from './reposervice.service';

describe('Reposervice.Service', () => {
  it('should create an instance', () => {
    expect(new Reposervice.Service()).toBeTruthy();
  });
});
